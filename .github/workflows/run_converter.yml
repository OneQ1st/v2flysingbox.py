name: 自动生成 Sing-box 规则

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    # cron 表达式格式：分 时 日 月 周
    # 0 2 * * * 表示 每天 2:00 (UTC) 运行
      - cron: '0 2 * * *'

jobs:
  convert_and_commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write 

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            v2fly2singbox.py

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Clone domain-list-community data (Sparse Checkout)
        run: |
          git clone --depth 1 --filter=blob:none --sparse https://github.com/v2fly/domain-list-community.git
          cd domain-list-community
          git sparse-checkout set data/
          # [修正点1] 确保使用 master 分支 (v2fly 仓库主分支名为 master)
          git pull origin master
          cd ..

      - name: Run Converter Script
        run: python v2fly2singbox.py
        
# 5. 提交生成的 JSON 文件
      - name: Commit generated files
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          # 更新提交信息
          commit_message: '🤖 Auto: Update individual sing-box rules'
          # [修正点] 使用通配符提交所有生成的 .json 文件
          file_pattern: '*.srs'
